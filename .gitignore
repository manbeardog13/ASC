# OS / editor cruft
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Node (only if you later add tooling — not needed for the app itself)
node_modules/

# Backup working directory produced by scripts/backup.sh in CI
out/
*.enc

# NOTE: js/config.js IS committed on purpose. It holds your Supabase URL and the
# public "anon" key, which are safe to publish (Row Level Security protects the
# data). GitHub Pages needs it in the repo to run the app.

# Generated cinematic gallery (unused by the app for now; heavy)
assets/gallery/
design/img/

# Local session handoff docs (contain internal notes; repo is public)
ASC_Handoff_*.md
design/asc-logo.png
design/asc-logo-small.png
